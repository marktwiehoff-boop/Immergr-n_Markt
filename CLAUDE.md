# CLAUDE.md — Immergrün Markt

This file provides context for AI assistants working on this repository.

## Project Overview

**Immergrün Markt** (Evergreen Market) — a new project. This repository is currently in its initial setup phase with no application code yet.

## Repository Info

- **Repository**: `marktwiehoff-boop/immergr-n_markt`
- **Default branch**: `main`
- **Language**: TBD — not yet established

## Getting Started

> **Note:** This project is freshly initialized. Update this section once the tech stack and tooling are chosen.

```bash
# Clone the repository
git clone https://github.com/marktwiehoff-boop/immergr-n_markt.git
cd immergr-n_markt

# Install dependencies (update once a package manager is chosen)
# npm install / pip install -r requirements.txt / etc.
```

## Project Structure

```
immergr-n_markt/
├── CLAUDE.md          # AI assistant context (this file)
└── (empty — project setup pending)
```

> Update this tree as the codebase grows.

## Development Workflow

### Branch Naming

- Feature branches: `feature/<short-description>`
- Bug fixes: `fix/<short-description>`
- Documentation: `docs/<short-description>`

### Commit Messages

- Use clear, descriptive commit messages
- Lead with a verb in imperative mood (e.g., "Add", "Fix", "Update", "Remove")
- Keep the subject line under 72 characters

### Pull Requests

- PRs should target `main`
- Include a summary of changes and a test plan
- Link related issues when applicable

## Build & Test

> **Placeholder** — fill in once the project tooling is established.

```bash
# Build
# <build command>

# Run tests
# <test command>

# Lint / format
# <lint command>
```

## Key Conventions

- Keep code clean and well-structured
- Follow the linting and formatting rules configured in the project
- Write tests for new functionality
- Do not commit secrets, credentials, or `.env` files

## Architecture

> **Placeholder** — document the architecture once it is established (e.g., frontend framework, backend services, database, deployment target).

## Environment Variables

> **Placeholder** — list required environment variables and their purpose here. Never commit actual secret values.

## Deployment

> **Placeholder** — document the deployment process once it is set up.

## AI Assistant Guidelines

When working in this repository:

1. **Read before writing** — always read existing files before modifying them
2. **Stay scoped** — only make changes that are requested; avoid unrelated refactors
3. **Test your changes** — run the test suite after making changes
4. **Don't commit secrets** — never commit `.env` files, API keys, or credentials
5. **Follow existing patterns** — match the code style and conventions already in use
6. **Update this file** — keep CLAUDE.md current as the project evolves
